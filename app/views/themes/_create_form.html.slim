= form_for [@website, @theme_new], remote: true do |form|
  div class="row"
    div class="col s12 center"
      h4 == t 'themes.new.invite'
    div class="col s12 input-field "
      a class="prefix tooltipped" data-position="right" data-delay="50" data-tooltip="#{t('themes.new.form.title.tip')}"
        i class="material-icons tip-icon" == 'create'
      = form.text_field  :title, :class=>"validate"
      = form.label       :title, "#{t('themes.new.form.title.label')}", :for=>"title"  
    div class="col s12 input-field "
      a class="prefix tooltipped" data-position="right" data-delay="50" data-tooltip="#{t('themes.new.form.intro.tip')}"
        i class="material-icons tip-icon" == 'create'
      = form.text_field  :intro, :class=>"validate"
      = form.label       :intro, "#{t('themes.new.form.intro.label')}", :for=>"intro"  
      = form.hidden_field :website_id, value: "#{@website.id}"
    div class="col s12 center"
      = form.submit 'OK', :class=>"btn"